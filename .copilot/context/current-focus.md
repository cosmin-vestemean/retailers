# Current Focus

## Last Updated 

- 2026-07-27 (RECADV/RETANN business manual fully re-verified against production; `FINDOCL`/`NUM04` semantics clarified; code-intersection review of `S1/JS/SALDOC_EF_27072026.js` — documentation-only session, no code changed)
- 2026-07-27 (official Infinite RECADV/RETANN v4.0 XML specifications received and mapped)
- 2026-07-27 (beneficiary EDInet Auchan/Dedeman billing specification extracted)
- 2026-07-21 (Infinite RETANN/RECADV enabled on live FTP; read-only verification)
- 2026-07-14 (Soft1 malformed JSON response diagnostics implemented locally)
- Session: Kaufland order mapping error investigation and response-parser hardening

## Current Goal

- **Next feature: do not lose any inbound EDI XML on Heroku ephemeral storage.** Target app is `retailers4` only; `retailers4` is planned to replace `retailers1`.
- Kaufland UI error `Mapping errors (1): Unexpected end of JSON input` is now locally hardened: the error originates in `buildOrderPayload()` when `runMappingSql` returns an empty/non-JSON HTTP body and raw `response.json()` throws. New shared `src/s1-response.js` reports endpoint + HTTP status + bounded response detail; it is used by `order-builder`, `order-sender`, `set-document`, and `CCCSFTPXML`. `test/edi/order-builder.test.js` covers empty and non-JSON mapping responses. Validation 2026-07-14: focused mapping/sender tests pass (9); `npm test` passes (50); `get_errors` clean. The exact June 18 XML is not present locally, so confirm the live `orders-log`/`CCCSFTPXML` evidence before changing a mapping or resending.
- `retailers4` Heroku config vars verified 2026-06-09: `DO_BUCKET=xml-edi-backup`, `DO_ACCESS_KEY` set, `DO_SECRET_KEY` set, `DO_ENDPOINT=https://fra1.digitaloceanspaces.com`.
- Signed DigitalOcean Spaces list test from local tooling succeeded against `xml-edi-backup` with HTTP 200, region `fra1`, key count 0.
- Cutover active 2026-06-09: `retailers1` scanner is disabled (`ENABLE_SFTP_SCANNER=false`, Heroku release `v374`). `retailers4` scanner is active (`ENABLE_SFTP_SCANNER=true`, `EDI_SCANNER=new`, `DO_RETRY_INTERVAL_MS=300000`, Heroku release `v83`). DO bucket `xml-edi-backup` was accessible immediately before cutover and `retry/` was empty (`retryTotal=0`).
- Implemented 2026-06-09: `@aws-sdk/client-s3` DO client + `do-storage` Feathers service; scanner backs up downloaded XML to DO only until `CCCSFTPXML.create` succeeds, then deletes the DO object. If insert into S1 fails, the object is moved/written under `retry/db-insert/...`; `DO_RETRY_INTERVAL_MS` starts a retry loop that re-inserts retry objects into `CCCSFTPXML` and deletes them from DO on success.
- UI implemented: `/app/do` route and navbar link `DO` after `Logs`; page shows bucket status, object list, XML view, manual retry, and delete.
- Validation 2026-06-09: `npm test` passes (31 tests); `npm --prefix frontend run build` passes; new SDK client status against real `retailers4` DO vars returns `accessible=true`, `retryObjects=0`.
- Live DO retry validation 2026-06-09 against `PetFactoryTEST` was run locally with `S1_BASE_URL=https://dev-petfactory.oncloud.gr/s1services` because Heroku `retailers4` still points `S1_BASE_URL` to production (`http://petfactory.oncloud.gr/s1services`). Test files were uploaded to real DO `retry/`, reinserted into `PetFactoryTEST.dbo.CCCSFTPXML`, deleted from DO, then processed to S1 docs: Mega `CCCSFTPXML=7166` -> `FINDOC=2135474`, `FINCODE=CKEY-00059788`, 1 line qty 6000; Auchan `7167` -> `2135475`, `CKEY-00059789`, 1 line qty 1152; Dedeman `7168` -> `2135476`, `CKEY-00059790`, 17 lines qty 633. Final DO `retry/` list is empty. `npm test` passes after the live test.
- Follow-up 2026-06-09: rows `7166..7168` had `FINDOC.NUM04=0` because the manual test changed EDI order numbers to alphanumeric `DORETRY...` values while `NUM04` is numeric. The DO retry test was rerun with numeric-only order numbers: Mega `CCCSFTPXML=7169` -> `FINDOC=2135477`, `FINCODE=CKEY-00059791`, `NUM04=1781013667106`, qty 6000; Auchan `7170` -> `2135478`, `CKEY-00059792`, `NUM04=1781013667117`, qty 1152; Dedeman `7171` -> `2135479`, `CKEY-00059793`, `NUM04=1781013667128`, qty 633. Final DO `retry/` list is empty; affected tests pass.
- Fix found during live validation: `src/services/CCCSFTPXML/CCCSFTPXML.class.js` strips the XML declaration before `createSftpXml` to avoid SQL Server XML parsing error `unable to switch the encoding`; `src/services/CCCSFTPXML/CCCSFTPXML.shared.js` now registers custom methods `claim` and `pending` used by the real scanner.
- Cutover attempt 2026-06-09: `main` switch commit `7e22ad0d` was deployed to `retailers1` as Heroku release `v371`; cutover was executed (`retailers1` off, `retailers4` on) but immediately rolled back after `retailers4` attempted to insert `ORDERS_*.pdf` files as XML. Current state after rollback: `retailers1` release `v373` with legacy scanner enabled; `retailers4` release `v78`, scanner disabled and retry loop disabled. Root cause fixed in `src/edi/scanner.js` commit `8a0cdc51`: scanner now accepts only `.xml`/`.confirm` files after prefix matching; `test/edi/scanner.test.js` adds a PDF guard case. `npm test -- --grep "EDI scanner against local ftp-srv"` invocation actually ran the full suite, 31 passing. `.main-worktree` commit `d197f38d` updates scripts/docs so cutover sets `DO_RETRY_INTERVAL_MS=300000` and rollback sets `DO_RETRY_INTERVAL_MS=0`.
- Duplicate order guard 2026-06-09: investigation found `NUM04=1833989` had two active order `CKEY` docs for Carrefour (`2123739` and duplicate `2144880`). `src/edi/order-sender.js` commit `6020d36f` now checks S1 for existing active order by `TRDR + NUM04 + SOSOURCE=1351 + FPRMS=701 + ISCANCEL=0` via `JSRetailers/runMappingSql` before `setDocument`. If found, it patches `CCCSFTPXML` to `SENT` with the existing `FINDOC`, logs `orders-log` operation `duplicateGuard` at warning level, and skips `setDocument`. If the guard lookup fails, it fails closed: marks `CCCSFTPXML` `ERROR`, logs `duplicateGuard` error, and does not create a document. Tests cover normal send, duplicate link, and guard lookup failure; `npm test` passes with 33 tests. Deployed to `retailers4` as Heroku release `v79`; scanner remains disabled.
- UI noise reduction 2026-06-09: `frontend/src/components/orders-table.js` now hides sent orders by default and suppresses red `XMLERROR` rendering for `SENT` rows whose message is `Duplicate NUM04 guard...`; `frontend/src/components/orders-log-table.js` hides resolved `duplicateGuard` warning rows by default, with a switch to show them. Duplicate context remains in `CCCSFTPXML.XMLERROR` and `orders-log`, but the UI stays focused on actionable rows. `npm --prefix frontend run build` passes.
- Manual scan routing 2026-06-09: dashboard `Scan Now` was still calling legacy `sftp.scanNow`, which logs `No files on server` / `No orders to create`. `frontend/src/services/api.js` now registers the `edi` Feathers service and routes manual `scanNow()` to `edi.scanNow`, matching the active `EDI_SCANNER=new` pipeline.
- New scanner heartbeat 2026-06-09: `src/edi/scanner.js` now writes one best-effort `orders-log` row at the end of a completed new scanner pass: `OPERATION='system'`, `LEVEL='info'`, `TRDR_RETAILER=-1`, with structured counters (`RETAILERI`, `XML_DESCARCATE`, `XML_INSERATE`, `DUPLICATE`, `DO_*`, `COMENZI_CREATE`, `MANUAL`, `ERORI`). No new DB table was added. `orders-log-table` and `JSRetailers.getOrdersLog` treat `system/info` as heartbeat, hidden by default unless the heartbeat switch is enabled or the user explicitly filters `Operation=System`. The AJS heartbeat filter also excludes legacy blank-operation/no-file/no-order info messages by `ISNULL(OPERATION,'')=''` and `MESSAGETEXT` patterns so server-side totals/pagination are not polluted by hidden rows.
- Logs FINCODE display 2026-06-09: `JSRetailers.getOrdersLog` now left-joins `CCCSFTPXML -> FINDOC` and returns `FINCODE`. `orders-log-table` uses it to display createDocument success logs as `FINCODE=... FINDOC=...` for old/new rows without mutating stored `MESSAGETEXT`. New scanner `order-sender` also resolves FINCODE best-effort after `setDocument` and includes it in future success logs.
- Old delivery-date guard 2026-06-09: commit `ed271c61` deployed to `retailers4` as release `v81` holds automatic order creation when `jsonOrder.DATA.MTRDOC[0].DELIVDATE` is before today's Europe/Bucharest date. The XML row is patched to `XMLSTATUS='MANUAL'`, `XMLERROR='Past delivery date guard...'`, `orders-log` gets `OPERATION='pastDeliveryGuard'`, `LEVEL='warn'`, and `setDocument` is not called. Scanner stats now include `held`. UI shows `MANUAL` rows as `Manual`, excludes them from bulk `Trimite toate`, but keeps individual `Send manual`. Manual sends mark `XMLSTATUS='SENT'` on success. Tests: `npm test` 35 passing; frontend build passes. Note: local `S1/JS/AJS/JSRetailers.js` now returns `XMLSTATUS/XMLERROR` from `getOrdersData`; copy this AJS to ERP before relying on the Manual badge/details in UI.
- Manual resend for mapping errors 2026-06-10: `frontend/src/components/orders-table.js` now shows an individual `Retrimite` button for rows with `XMLSTATUS='ERROR'`; bulk `Trimite toate` remains restricted to `pending` rows. Use this after correcting mapping data in S1. Validation: `npm --prefix frontend run build` passes.
- Order list OrderId fallback 2026-06-10: `JSRetailers.getOrdersData` now resolves `OrderId` as DocProcess `/Order/ID` first, then Infinite `/Document/Order/OrderHeader/BuyerOrderNumber`, so the same orders list shows the retailer-specific order number consistently. Mirrored in `S1/SQL/getOrdersData.sql` and `mcp-soft1/index.js` for parity. Validation: `get_errors` passes on all three files. Requires copying updated `S1/JS/AJS/JSRetailers.js` to ERP AJS before the live UI picks up the Infinite fix.
- Infinite prefix routing fix 2026-06-10: `AUCHAN_181792345.xml` was inserted under Dedeman (`CCCSFTPXML=7305`, `TRDR_RETAILER=11654`) because `infiniteProvider.filenamePrefixes()` returned both `AUCHAN_` and `DEDEMAN_` for every Infinite config. Commit `6f282a27` passes the CCCSFTP row to `filenamePrefixes()` and maps `13248 -> AUCHAN_`, `11654 -> DEDEMAN_`; regression test covers Dedeman-first/Auchan-second routing. Existing bad row should be moved to `TRDR_RETAILER=13248`, reset to `XMLSTATUS='NEW'`, and `XMLERROR=''`, or deleted/reinserted as Auchan.
- Manual/bulk send backend fix 2026-06-10: UI `Send`, `Send manual`, `Retrimite`, and `Trimite toate` all call `sftp.sendStoredOrder`. That method now uses the new `buildOrderPayload` + `sendOrderToS1` path instead of legacy `createOrderJSON`, fixing Infinite XML failures like `Cannot read properties of undefined (reading 'length')` from empty legacy `ITELINES` grouping. Validation: `npm test` passes (36 tests).
- APERAK separation 2026-06-10: APERAK response files do not belong in `CCCSFTPXML` order rows. `getOrdersData` and dashboard `retailer-stats` now exclude `APERAK_%` / `EDIDOCTYPE='APERAK'`, and the new scanner stores DocProcess `aperak` downloads directly into `CCCAPERAK` by parsing `DXMessage` fields and linking to invoice `FINDOC` by `FINCODE`. Regression test covers APERAK SFTP fixture stored in `CCCAPERAK`, not `CCCSFTPXML`. Existing prod data check found 27 APERAK rows in `CCCSFTPXML`; all can be linked globally to invoice FINDOCs (3 Metro rows were saved under Carrefour but resolve to Metro invoices). Migration script: `scripts/migrate-aperak-from-cccsftpxml.sql` (review mode with `ROLLBACK` by default). Validation: `npm test` passes (40 tests).
- APERAK DO retry root cause 2026-06-11: DocProcess APERAK rows could stay in DO `retry/db-insert/...` with `S1:Exception:Ole Error: 80040E07. Operand type clash: ntext is incompatible with date` because `JSRetailers.createAperak` inserted `XMLSENTDATE`, `MESSAGEDATE`, and `MESSAGETIME` as raw string params into `CCCAPERAK` `DATE/TIME` columns. Local fix in `S1/JS/AJS/JSRetailers.js` now casts placeholders to `VARCHAR` and uses `TRY_CONVERT(DATE/TIME, ...)` inside the `INSERT`. Static validation: `get_errors` clean on the AJS file. Requires manual copy of the updated AJS into ERP before live APERAK retries will succeed.
- APERAK retry fully fixed 2026-06-11: after the user deployed the AJS, a live probe to `JSRetailers/createAperak` with a date/time payload returned `success:true` (no ntext error) and the probe row was confirmed inserted with correct `MESSAGEDATE='2026-06-10'`/`MESSAGETIME='17:50:16'` (`CCCAPERAK=7010`, since deleted). Two remaining issues were fixed: (1) `createAperak` previously returned `CCCAPERAK:0` because `X.SQL('SELECT SCOPE_IDENTITY()')` ran outside the `X.RUNSQL` connection scope — now the `INSERT` uses `OUTPUT INSERTED.CCCAPERAK` via a single `X.SQL` call so the real PK is returned (requires re-copy of AJS to ERP). (2) The real reason retained APERAKs never cleared: `src/edi/do-retry.js` `retryDoObject` only handled `doctype==='ORDERS'` and skipped `APERAK`, so APERAK objects were never reprocessed/deleted from DO and kept their historical error metadata. `do-retry.js` now branches to `retryAperakObject` which rebuilds the provider from metadata and calls the now-exported `insertAperakRow` from `src/edi/scanner.js`, then deletes the DO object on success (and also on dedupe `false`). `get_errors` clean on both files. The retained `retry/db-insert/...` APERAK objects will clear on the next 5-minute DO retry loop pass (or via the manual `Retry` button) once this `do-retry.js` change is deployed and the updated AJS is in ERP.
- Invoice pending status 2026-06-10: A positive APERAK (`DOCUMENTRESPONSE` `RECEPTIONAT` or `ACCEPTAT`) means the invoice is no longer pending send, even if `MTRDOC.CCCXMLSendDate` is still null. Dashboard `Facturi de trimis` now excludes invoices with positive APERAKs, and invoice table treats any positive APERAK row as confirmed so `Trimite toate` will not resend it. UI also normalizes `CCCXMLSendDate` field casing from AJS responses. Validation: `npm --prefix frontend run build` passes; focused `retailer-stats service` test passes.
- Invoice sent visibility 2026-06-10: invoice tab now mirrors orders with an `Arată trimise` switch. Default view requests only invoices still pending send (`CCCXMLSendDate IS NULL` and no positive APERAK); switching on includes sent/APERAK-confirmed invoices. Sent rows show `Resend` only, not the normal `Send` action. Requires copying updated `S1/JS/AJS/JSRetailers.js` to ERP because `getInvoicesData` now accepts `includeSent`.
- Invoice sent date 2026-06-10: marking an invoice as sent now calls backend service `mark-invoice-sent`, which calls AJS `JSRetailers.markInvoiceSent`; the AJS endpoint executes `UPDATE MTRDOC SET CCCXMLSendDate = GETDATE() WHERE FINDOC = :1` and returns the server timestamp. This replaces the previous browser-generated timestamp sent through `setDocument`. User manually copied `JSRetailers` to ERP; no-op verification with `findoc=0` returned `Invalid FINDOC.`, confirming endpoint availability. Validation: frontend build passes; focused `mark-invoice-sent service` test passes.
- Invoice sent-date reload fix 2026-06-10: `FAEX-PF-38362` / `FINDOC=2133941` had `MTRDOC.CCCXMLSendDate='2026-06-10 12:05:51'` in S1, but live `getInvoicesData(includeSent=true)` returned `CCCXMLSendDate:null`, so UI showed it as unsent after refresh. After copying the first AJS fix, live endpoint still returned null. Final local fix: `getInvoicesData` selects `CONVERT(VARCHAR(19), m.CCCXMLSendDate, 120) AS CCCXMLSendDate` and returns that string via `getDatasetFieldValue()`, avoiding Soft1 datetime serialization entirely. Live SQL probe through `processSqlAsDataset1` confirms the converted value returns `2026-06-10 12:05:51`. Requires copying updated `S1/JS/AJS/JSRetailers.js` to ERP AJS again, then rechecking live `getInvoicesData`. Validation: `npm --prefix frontend run build` passes; `get_errors` on AJS passes.
- Invoice list count 2026-06-10: Supeco dashboard showed 35 pending invoices while invoice table displayed `Trimite toate (25)`. Live `getInvoicesData` returned `total:35`; page 2 contained the remaining 10 invoices, so the mismatch was pagination, not SQL filters. Updated `frontend/src/components/invoice-table.js` so `_sendAllUnsent()` fetches all pending invoice pages with `includeSent:false` before sending, and `_unsentCount` uses `_total` when the sent switch is off. Validation: `npm --prefix frontend run build` passes; `get_errors` on invoice-table passes.
- XML mapping UI/DB cleanup 2026-06-10: `frontend/src/components/xml-mapping-table.js` now scopes overwrite checks by `TRDR_RETAILER`, filters loaded rows to the selected `CCCDOCUMENTES1MAPPINGS`, and deduplicates loaded/saved rows so repeated UI rows do not get reinserted. Live DB check found real duplicates only in `PetFactory.dbo.CCCXMLS1MAPPINGS` for Dedeman ORDER doc `CCCDOCUMENTES1MAPPINGS=43` (`TRDR_RETAILER=11654`): exact duplicate pairs `1336/1345`, `1337/1346`, `1338/1347`, `1339/1348`, `1340/1349`, `1341/1350`, `1342/1351`; `SellerID` also appears twice as `1343` (`MANDATORY=0`) and `1352` (`MANDATORY=1`). `PetFactoryTEST.dbo.CCCXMLS1MAPPINGS` has no duplicates.
- Legacy flow migration 2026-06-10: frontend active flows no longer call `client.service('sftp')`. `downloadAndStoreOrders()` now triggers `edi.scanNow`; order manual/bulk send uses new `edi-orders.create`; invoice upload uses new `edi-invoices.create` with SFTP private key held in memory; invoice `Download APERAKs` uses new `edi-aperaks.create`. Legacy `sftp.sendStoredOrder` now delegates to `edi-orders` for temporary compatibility. APERAK storage was hardened to handle no local folder/files, dedupe by `DOCUMENTUID + response/detail`, and store unlinked APERAKs in `CCCAPERAK`. Remaining legacy backend code: `sftp.downloadXml/storeXmlInDB/storeAperakInErpMessages/uploadXml/createOrders/createOrderJSON/sendOrderToServer/scanAndSend` and the `EDI_SCANNER=legacy|both` scheduler branch. Validation: `npm test` passed (48 tests); `npm --prefix frontend run build` passed; search confirms no `client.service('sftp')` in `frontend/src` or built `public/dist` bundle.
- DO retry experiment 2026-06-10: user-provided local XMLs from `10062026/Dedeman+Auchan` were filtered to 5 eligible orders (Auchan `181788970`, `181792261`; Dedeman `181777164`, `181777730`, `181783566`). Excluded Auchan `181791994` and `181792345` because their GLNs are not served / missing in S1. Uploaded the 5 XMLs to DO under `retry/experiment-2026-06-10/...`, ran `do-storage.retry()` successfully (`processed=5`, `succeeded=5`, retry prefix empty after delete), then ran `scanAll()`. Result: `CCCSFTPXML` rows `7309..7313` all `SENT`; S1 docs created: Dedeman `2145255` `CKEY-00060087` NUM04 `4516609102` 17 lines qty 579; Dedeman `2145256` `CKEY-00060088` NUM04 `4516609374` 1 line qty 120; Dedeman `2145260` `CKEY-00060091` NUM04 `4516609639` 36 lines qty 481; Auchan `2145257` `CKEY-00060089` NUM04 `1368726` 4 lines qty 144; Auchan `2145259` `CKEY-00060090` NUM04 `5702655` 1 line qty 24. Same `scanAll()` also downloaded an unrelated live DocProcess row `CCCSFTPXML=7314` for retailer `11322`, which remains `ERROR` due missing `TRDBRANCH=4049728610005` and `MTRL=20848803`; not part of this experiment.
- DocProcess shared-inbox routing fix 2026-06-10: `CCCSFTPXML=7314` is actually Kaufland (`TRDR=12349`, GLN `4049728610005`, order `3200159108`) but was inserted under Carrefour (`TRDR=11322`) because all DocProcess configs share the same `ORDERS_` inbox and scanner processed configs in TRDR order. `src/edi/scanner.js` now resolves DocProcess order retailer after download by parsing XML GLN and looking up `TRDBRANCH.CCCS1DXGLN` among active DocProcess retailers before inserting/backup metadata. Follow-up decision: no ambiguity allowed. If DocProcess XML parsing, GLN extraction, active-candidate lookup, or GLN match fails, scanner fails closed with `DocProcess routing error...`; it does not insert under the current config retailer. Latest implementation stores unresolved DocProcess orders in `CCCSFTPXML` as `TRDR_RETAILER=0`, `XMLSTATUS='ERROR'`, `XMLERROR='DocProcess routing error...'`, and rich routing context in `JSONDATA.routing` (buyer, seller, delivery point, GLNs, dates, line count, item preview). The frontend has `/app/routing-errors` (`Erori rutare`) with reprocess/view XML/delete actions. `CCCSFTPXML.resolveRouting(id)` re-parses XML and, after GLN is corrected in S1, patches the row to the resolved `TRDR_RETAILER`, `XMLSTATUS='NEW'`, and clears `XMLERROR`; unresolved retry attempts update the row error/context. Regression tests cover Carrefour-first/Kaufland-second routing and no-match stored routing-error behavior. Validation: `npm test` passes (39 tests); `npm --prefix frontend run build` passes. Existing bad row `7314` still needs data correction if it should be processed: move to `TRDR_RETAILER=12349`, reset `XMLSTATUS='NEW'`, clear `XMLERROR`.

- **EDI refactor validated end-to-end on dev S1 / PetFactoryTEST with real DocProcess and Infinite/Dedeman XMLs.**
- Strategy: local `ftp-srv` (Infinite) + local SFTP server (DocProcess) serving anonymized fixtures from `test/fixtures/`. Real Infinite directory layout has been mapped from prod (read-only LIST + safe pulls from already-consumed folders).
- `dev-web.xco` has been created and points to `PetFactoryTEST`; `JSRetailers.js` has been copied into ERP AJS.
- `PetFactoryTEST.dbo.CCCRETAILERSCLIENTS.WSURL` now points to `https://dev-petfactory.oncloud.gr/s1services` for the active company 50 row.
- Final MCP `CSTINFO.SODATA` hash check confirms `JSRetailers` in both `PetFactory` and `PetFactoryTEST` matches the current local `S1/JS/AJS/JSRetailers.js` file.
- Live dev validation 2026-06-05 succeeded using real XML `documentatie/ORDERS_DX01_144_20260403_01004728_VAT_RO17275880_DX.xml` with only order id/UUID changed to avoid duplicate: `CCCSFTPXML=7148`, `FINDOC=2135459`, `FINCODE=CKEY-00059773`, `XMLSTATUS=SENT`, `SERIES=7012`, `TRDR=12649`, line `MTRL=44857`, `QTY1=6000`, `PRICE=0.85`.
- After the SODATA compare, local `JSRetailers.js` was updated again: filename filters now use `CAST(XMLFILENAME AS VARCHAR(MAX))`, and `createSftpXml` falls back to lookup by filename+retailer when `SCOPE_IDENTITY()` returns 0. User copied this updated AJS into both ERP databases; MCP hash check confirms both match the local file.
- Live Infinite/Dedeman validation 2026-06-05 succeeded using real XML `documentatie/infinite_samples/orders/DEDEMAN_181059380.xml` with only `BuyerOrderNumber` changed to avoid duplicate: `CCCSFTPXML=7149`, `FINDOC=2135460`, `FINCODE=CKEY-00059774`, `XMLSTATUS=SENT`, `SERIES=7012`, `TRDR=11654`, 17 lines, total quantity 633.
- For Infinite/Dedeman testing, 7 XML mapping rows were added in `PetFactoryTEST` under existing `CCCDOCUMENTES1MAPPINGS=43`: `Order/OrderHeader/RequestedDeliveryDate`, `BuyerOrderNumber`, `OrderIssueDate`, `ShipToParty/GLN`, `UnitNetPrice`, `QuantityOrdered`, `BuyerItemID`.
- After the Infinite/Dedeman create test, local `JSRetailers.js` was updated again: filename comparisons now cast both sides (`CAST(XMLFILENAME AS VARCHAR(MAX)) = CAST(:1 AS VARCHAR(MAX))`) because Soft1 binds filename parameters as `ntext`. User copied this newest local AJS into both ERP databases; MCP hash check confirms both match the local file.
- Auchan/Dedeman mapping parity is complete in both `PetFactory` and `PetFactoryTEST`: `CCCSFTP` has separate Infinite rows for Dedeman `11654` and Auchan `13248`; ORDER mappings for both retailers have 7 XML rows each; duplicate Auchan TEST XML rows were removed; no duplicate XML mappings remain.
- Dashboard card for Auchan `13248` was added in `frontend/src/state/app-context.js` using the user-provided logo URL. Dedeman card already existed. `npm --prefix frontend run build` passes.
- Real Auchan order samples found and verified: `documentatie/infinite_samples/orders/AUCHAN_180887320.xml`, `AUCHAN_180887321.xml`, `AUCHAN_180887322.xml`, plus older `documentatie/dedeman/AUCHAN_145963053.xml`. Their ShipTo GLNs and BuyerItemIDs resolve for TRDR `13248` in `PetFactoryTEST`.
- Controlled Auchan dev tests succeeded. Synthetic non-numeric test: `CCCSFTPXML=7152`, `FINDOC=2135461`, `FINCODE=CKEY-00059775`, `XMLSTATUS=SENT`, `TRDR=13248`, `TRDBRANCH=1358`, `NUM04=0`, line `MTRL=27912`, `QTY1=2`, `PRICE=1.72`. Numeric test: `CCCSFTPXML=7153`, `FINDOC=2135462`, `FINCODE=CKEY-00059776`, `XMLSTATUS=SENT`, `NUM04=1780670762`, same branch/line mapping.
- Real Auchan dev validation completed for all four samples. First batch: `CCCSFTPXML=7154..7157`, `FINDOC=2135463..2135466`, all `SENT`. Second duplicate test batch was also sent accidentally: `CCCSFTPXML=7158..7161`, `FINDOC=2135467..2135470`, all `SENT`. Headers validated (`TRDR=13248`, `SERIES=7012`, correct `TRDBRANCH`, `DATE01`, `DELIVDATE`, numeric `NUM04`); line counts validated: 1, 1, 4, and 3 lines respectively, with expected MTRL/QTY/PRICE mappings.
- Real Dedeman dev validation completed for all folder samples: `documentatie/infinite_samples/orders/DEDEMAN_181017305.xml`, `DEDEMAN_181017333.xml`, `DEDEMAN_181059380.xml`. Results: `CCCSFTPXML=7162..7164`, `FINDOC=2135471..2135473`, `FINCODE=CKEY-00059785..00059787`, all `SENT`, `TRDR=11654`, `SERIES=7012`, numeric `NUM04=17806713461..17806713463`; line counts 12, 57, 17 with total quantities 292, 1325, 633.

## Active Area

- New module: `src/edi/` (transports, providers, scanner, order-builder, order-sender, sign-smime)
- New Feathers service: `src/services/edi/` (exposes `scanNow`, `scanPeriodically`, `stop`)
- AJS extensions in `S1/JS/AJS/JSRetailers.js`: `listEdiConfigs`, `claimSftpXml`, `getPendingSftpXml`, `runMappingSql`, extended `patchSftpXml`, `createSftpXml`/`getSftpXml` now include EDIDOCTYPE.

## Relevant Files

- `src/edi/scanner.js` — orchestrator with concurrency lock; downloads + dedupes + processes NEW rows.
- `src/edi/order-builder.js` — `buildOrderPayload` (parameterized {value} substitution via `runMappingSql`).
- `src/edi/order-sender.js` — `sendOrderToS1` with retry + status machine NEW→PROCESSING→SENT|ERROR.
- `src/edi/transports/{sftp,ftp}-transport.js` + factory.
- `src/edi/providers/{docprocess,infinite}.provider.js` + factory.
- `src/edi/sign-smime.js` — PKCS#12 → detached S/MIME signing for Infinite invoice upload (uses env vars `EDINET_P12_BASE64`, `EDINET_P12_PASSWORD`).
- `src/services/CCCSFTP/CCCSFTP.class.js` — new `list({onlyActive})` calling `listEdiConfigs`.
- `src/services/CCCSFTPXML/CCCSFTPXML.class.js` — new `claim(id)`, `pending({...})`; `patch` accepts `XMLSTATUS`, `XMLERROR`.
- `src/services/CCCSFTPXML/CCCSFTPXML.class.js` — `create` now falls back to `{ CCCSFTPXML: result.id, ...data }` when AJS returns empty `data`.
- `src/services/edi/{edi.class.js,edi.service.js}` — Feathers service.
- `src/app.js` — scanner bootstrap is now opt-in: `ENABLE_SFTP_SCANNER=true` is required before either pipeline starts; `EDI_SCANNER` (`new`|`legacy`|`both`) selects the pipeline only after that.
- `frontend/src/state/app-context.js` — dashboard retailer card list; now includes Auchan `13248` and Dedeman `11654`.

## Confirmed Decisions

- Status machine on CCCSFTPXML.XMLSTATUS: `NEW → PROCESSING → SENT | ERROR`; atomic claim via SQL `UPDATE ... WHERE XMLSTATUS='NEW'`.
- EDIDOCTYPE column populated on insert (`ORDERS`|`RETANN`|`APERAK`).
- CCCEDIPROVIDER extended with `CODE varchar(20)`, `ISACTIVE bit`; rows updated (docprocess, infinite).
- SQL injection on `{value}` mapping templates closed via AJS `runMappingSql` (rewrites `'{value}'` → `:1` and binds parameter; no DB template migration needed).
- Hardcoded S1 base URLs have been removed from `src/**`. Base URL resolution is now config-driven: explicit request URL when provided, else app config `s1BaseUrl`, else env `S1_BASE_URL`. The EDI order flow still prefers retailer-specific `CCCRETAILERSCLIENTS.WSURL`.
- Scanner safety is stricter now: `ENABLE_SFTP_SCANNER` defaults to `false`, and both automatic scheduling and manual `scanNow` endpoints short-circuit unless explicitly enabled.
- `privateKey.txt` no longer written to disk — `SftpTransport` holds the key in-memory.
- Legacy `sftp` service NOT deleted yet; toggled via `EDI_SCANNER=legacy` for fallback during validation.
- New order scanner constants now match the active ORDER mappings in PetFactoryTEST: `SOSOURCE=1351`, `FPRMS=701`, `SERIES=7012`.
- `buildOrderPayload` now groups line fields into real `ITELINES` rows and sets `SALDOC[0].SERIES`/`SALDOC[0].TRDR`, matching legacy payload behavior.
- MCP direct SQL currently connects to `PetFactory`, but `PetFactoryTEST` is visible on the same server (`PET-PRI-S1`) and can be queried cross-database as `PetFactoryTEST.dbo.*`.
- **Provider split (user-confirmed 2026-06-05): Auchan and Dedeman are on Infinite Edinet; all other retailers are on DocProcess.**
- **Cutover strategy (user): production = `retailers1` (branch `main`, Heroku `retailers1`) with scanner ACTIVE. `retailers4` (branch `feat/edi-safety-sftp-tests`, Heroku `retailers4`) keeps scanner OFF via `ENABLE_SFTP_SCANNER=false` so the two apps never steal each other's files. When `retailers4` tests are trusted, add the same kill-switch to `retailers1` to stop its scanner, then let `retailers4` take over.**

## Test harness (DONE)

- `ftp-srv` installed (dev dep). Local FTP server bound to `127.0.0.1`.
- `src/edi/transports/safe-host.js` — enforces loopback-only hosts when `NODE_ENV=test` (override: `EDI_ALLOW_PRODUCTION_HOSTS=1`). Wired into both `FtpTransport` and `SftpTransport` constructors.
- `test/edi/fixtures/infinite/orders/` — 2 anonymized fixtures (`AUCHAN_900000001.xml`, `DEDEMAN_900000002.xml`). Layout mirrors real prod (also empty `retanns/`).
- `test/edi/fixtures/docprocess/out/ORDERS_TEST_DX01_900000001.xml` — anonymized DocProcess UBL fixture (TRDR/MTRL/GLN scrubbed).
- `test/edi/ftp-server.js` — reusable FTP harness (`startFtp({port,root})`, `makeWorkdir()`).
- `test/edi/sftp-server.js` — reusable SFTP harness via `ssh2.Server` + cached 2048-bit RSA host key. Implements REALPATH/STAT/OPENDIR/READDIR/OPEN/READ/WRITE/CLOSE/MKDIR/REMOVE. Password auth only. Refuses to start outside `NODE_ENV=test`.
- `test/edi/scanner.test.js` — 4 tests passing (FTP/Infinite scanner).
- `test/edi/sftp-scanner.test.js` — 2 tests passing (SFTP/DocProcess scanner: safe-host refusal + real SFTP download/insert).
- `test/edi/order-builder.test.js` — 2 tests passing (mocked `runMappingSql` via injected `fetchImpl`; verifies SQL field substitution + error-path logging).
- `test/edi/order-sender.test.js` — 1 test passing (propagates dev/prod S1 target URL to `setDocument`, marks CCCSFTPXML row `SENT`).
- `test/edi/scanner-flags.test.js` — 3 tests passing (default scanner disabled + manual scan guards on both new and legacy services).
- `test/s1-base-url.test.js` — 3 tests passing (explicit URL, app config fallback, missing-config failure).
- **Total: 15 passing tests covering EDI + S1 base URL resolution, all under NODE_ENV=test, all loopback-only.**
- Fixed `FtpTransport.list` modifyTime bug (Date(0) → undefined).
- `SftpTransport` now accepts `password` as fallback when no `privateKey` (for test use; prod path unchanged).
- `buildOrderPayload` accepts optional `fetchImpl` (defaults to `node-fetch`) for test injection.
- `connectToS1`, `setDocument`, and the remaining server-side S1 service classes now resolve the base URL through the shared helper instead of embedding the production endpoint in each file.
- Production data sources kept gitignored under `documentatie/infinite_samples/`.

## Infinite FTP layout (real, mapped 2026-05-26)

Root `/` contains 4 top-level inbound/outbound trees:

- `/orders/` — INBOUND orders from retailers (Dedeman + Auchan share this account)
  - root = un-consumed files (server moves on RETR; LIST is safe)
  - `sent/` — post-consumption archive (DEDEMAN_*.xml, AUCHAN_*.xml) ← safe source for fixtures
  - `confirm/send/` — outbound .confirm acks we wrote back (e.g. `DEDEMAN_181059380.xml_417.confirm`)
  - `confirm/{archive,duplicate,error,logs/{err,ok},omit,temp}`
  - `failed/`, `temp/`, `omitted/`, weird `AUCHAN*` literal dir (empty)
- `/retanns/` — INBOUND return announcements; currently empty in prod
- `/desadv/` — OUTBOUND dispatch advice (Pet Factory → retailer)
  - `archive/` AEX-AE-*.xml (98), `logs/ok/` MessageAcknowledgement_*.xml
- `/invoice/` — OUTBOUND signed invoices (FAEXD-PF-*.xml in `archive/`, acks in `logs/ok/`)

Fixtures seeded under `documentatie/infinite_samples/` (gitignored):
`orders/`, `orders_confirm/`, `desadv_acks/`, `desadv_archive/`, `invoice_archive/`, `invoice_acks/`.

Key behavioral notes:
- Server move-on-RETR semantics: NEVER call `transport.download()` against real `/orders/` root in tests. LIST is safe.
- Naming proves `infiniteProvider.filenamePrefixes('orders') = ['AUCHAN_', 'DEDEMAN_']` is correct.
- `provider.remoteSubdir('retann')` → `/retanns/` (plural) matches real layout.
- No `/recadv/` or `/aperak/` folder on Infinite — RECADV/APERAK not part of this provider's flow; ignore for now.
- **Live change verified 2026-07-21 at 16:27 UTC using recursive FTP `LIST` only (no download/consumption).** Root `/` now has 5 dirs: `desadv`, `invoice`, `orders`, `recadv`, `retann`. This confirms EDINet enabled both RECADV and RETANN delivery on the existing FTP account.
- **Archive search 2026-07-27, FTP `LIST` only:** both Dedeman and Auchan `CCCSFTP` rows resolve to the same Infinite account. `/recadv` now has 91 numeric files and only an empty `temp/` child; it has no `archive`, `sent`, `failed`, or log tree. `/retann` now has 5 numeric files; its `sent/`, `failed/`, `temp/`, and `confirm/archive/` directories are empty. Therefore no safely reusable archived RECADV/RETANN payload exists on this account. Archives are populated only for outbound flows (`/desadv/archive`: 65 files in this listing; `/invoice/archive`: 156 files) and contain DESADV/invoice XML, not inbound receipt/return payloads. Do not `RETR` the numeric root files without the controlled immediate-DO-backup procedure.
- **DB check 2026-07-14**: `SELECT ... FROM CCCSFTPXML WHERE EDIDOCTYPE IN ('RETANN','RECADV') OR XMLFILENAME LIKE '%RETANN%' OR XMLFILENAME LIKE '%RECADV%'` returns **zero rows**. No RETANN/RECADV document has ever been stored in `CCCSFTPXML` for any retailer.
- Current scanner will not ingest either newly enabled flow. Live RETANN path is singular `/retann`, while `infiniteProvider.remoteSubdir('retann')` returns `/retanns/`; the numeric filenames also fail the current `AUCHAN_`/`DEDEMAN_` prefix filter. RECADV maps to `/recadv/`, but `filenamePrefixes('aperak')` returns `[]`, so the scanner skips it, and `parseAperak()` is still a raw-tree stub incompatible with `insertAperakRow()`'s normalized APERAK fields. Do not enable downloads until routing, parsing, dedupe, DO backup, and Soft1 destination semantics are implemented and tested; Infinite FTP may consume/move files on `RETR`.
- **Beneficiary specification extracted 2026-07-27** from `documentatie/Fluxuri complete EDInet Auchan-Dedeman/Manual_integrare_facturare_edi_Auchan_Dedeman.docx`; normalized technical version is beside it as `.md`. RECADV is the EDInet receipt confirmation that drives billing reconciliation against Soft1 dispatch advice `7111`, not an APERAK. Standard receipts invoice directly (`7122` Auchan, `7123` Dedeman); shortages create return advice `9221` and invoice only accepted quantities. Dedeman matching is by EAN validated through `CCCS1DXTRDRMTRL`; Auchan matching is by customer item code through the same table, never trusted EAN. Dedeman surplus creates a later `Doar aviz` order that stays in Retailers, bypasses normal CKEY creation, is advised directly on `7111`, then invoiced on `7123`. A Dedeman receipt may consolidate multiple advice/order references into one `7123` invoice. Returnable EURO pallets are never invoiced. Exact XML fields and Soft1 document-link creation remain unknown until a representative RECADV payload is obtained.
- **Soft1 example validation 2026-07-27** is documented in `documentatie/Fluxuri complete EDInet Auchan-Dedeman/Analiza_exemplelor_in_Soft1.md` (MCP read-only against production). Auchan `1436603` confirms `7012 -> 7111 -> 7122 + 9221`, with product `MTRL=40977`, customer code `363360`, quantities `417 -> 416 + 1`. Dedeman `4516724271` confirms `96 -> 48 invoiced + 48 on 9221`, net shortage `47.04`, and line-level source links. Consolidation invoice `FAEXD-PF-39304` has 34 lines sourced from two advice docs (29 + 5), exactly 894 units / 4404.79 net. Critical corrections: active ORDER mappings for both retailers resolve `BuyerItemID` through `CCCS1DXTRDRMTRL.CODE`; Dedeman example EAN `5949060224108` is `MTRL.CODE1`, while its customer code is `7073512`. Series `7531` is officially `Retur Factura vanzari (QV)`, not RECADV. The real `DOAR AVIZ` order `4516747570` was integrated as CKEY despite the manual's desired rule, generated a new advice/invoice, and the old direct invoice was reversed with 7531. Beneficiary must choose the future rule before implementation.
- **User insight 2026-07-14**: beneficiary showed the Infinite Edinet **web portal** (not the FTP account) where he can open RECADV (goods-receipt advice, with +/- quantity differences) and RETANN (return) documents. His employees currently print these and enter them manually into Soft1. This confirms RECADV/RETANN are delivered to the beneficiary through a channel other than our `pet_factory_ro` FTP account (web portal only, possibly a different account/API). Brainstormed next steps (not yet started): (1) ask Infinite/EDI admin whether RECADV/RETANN can be enabled for FTP delivery on our existing account; (2) check for an official Infinite web-service/API behind the portal; (3) as a fallback, inspect portal network calls (needs beneficiary portal credentials) or consider browser automation (Playwright) to fetch documents if no official channel exists. Explicitly out of scope for now: DocProcess RECADV investigation (user said not interested yet).
- **Public site check 2026-07-14**: fetched `infinite-it.com/en` and `infinite.pl` (same vendor, global rebrand of the Polish `ftp.infinite.pl` company). Both are marketing-only sites (feature lists, compliance blog posts, generic FAQ); no technical docs, no API reference, no info on which document types are FTP vs. portal-only. Confirms this question can only be answered by contacting Infinite support/account manager directly, not via public research.
- **Official EDInet Connector manual reviewed 2026-07-14** (`documentatie/EDInet_Connector/EDInet Connector.md`, a 2012-era Java desktop FTP-sync tool from Infinite, superseded by our direct Node FTP client — kept only as protocol/folder-structure reference). Table of contents documents exactly 3 relation types: **Orders, Retanns, Invoices**. No RECADV relation type exists anywhere in the manual. Retanns section confirms `Remote directory: /retanns/`, same host `ftp.infinite.pl`, port 21 — matching our current code and the live empty `/retanns/` folder exactly. This is independent confirmation (from Infinite's own official docs, not just live probing) that: (a) RETANN's FTP path/mechanism in our code is already correct, and (b) RECADV has never been a supported FTP relation type for this connector — supporting the theory that RECADV reaches the beneficiary through a separate web-portal-only channel, not FTP. **Superseded 2026-07-27** on point (b): Infinite issued an official RECADV XML specification and the live account now exposes `/recadv`, so RECADV is a supported document type; only the 2012 connector predates it.
- **Official Infinite XML specifications received 2026-07-27**: `RECADV ... VERSION 4.0 - EDInet XML`, `RETANN ... VERSION 4.0`, plus `XML_DESADV_v4.1.pdf` / `XML_DESADV_v4.pdf`. **Canonical normalized reference is `documentatie/dedeman/Infinite_EDInet_DESADV_RECADV.md`** (DESADV v4.1 + RECADV v4.0 + RETANN v4.0, field by field). The business manual `documentatie/Fluxuri complete EDInet Auchan-Dedeman/Manual_integrare_facturare_edi_Auchan_Dedeman.md` now only summarizes the billing-relevant conclusions and links to it, to avoid duplicated schema tables. A spec-sample RETANN payload already exists locally at `documentatie/dedeman/RetAnn.xml` (Infinite's own example, `TEST SRL` — usable as a fixture template, NOT as validation of the delivered format). Key structural facts:
  - RECADV root is `<Document>` with `RecadvHeader` / `RecadvParty` / `RecadvDetail` / `RecadvSummary` / `DocumentSummary`. RETANN root is `<Retann>` and uses `<ILN>` instead of `<GLN>`, plus nested `<AddressDetails>` — the two schemas are NOT interchangeable and neither matches the existing `<Document><Order>` order schema.
  - `BuyerItemID` is mandatory in both; `GTIN`/`EAN` is optional. This resolves the Dedeman EAN-vs-customer-code contradiction: matching is `BuyerItemID -> CCCS1DXTRDRMTRL.CODE` for BOTH retailers, identical to the active ORDER mappings. EAN stays a secondary check only.
  - Header `DeliveryDocumentNumber` is length-limited and demonstrably truncated in Infinite's own example (`DI-00006331/DI-0`), while `Item/BuyerOrderNumber` is mandatory per line. Therefore **lines are authoritative and the header is only a consolidation hint** — this resolves the open grouping-key question.
  - RETANN quantities are negative and `RetannRefDoc` carries both `OrderAtBuyerParty/DocID` and `DesadvParty/DocID`, giving a direct link to our `7111` advice.
  - `DocumentSummary/NumberOfDocuments` implies one file may contain multiple documents; the specs also contain typos (`</MonetaryNetvalue>`, `<//DocID>`), so parsers must be tolerant and validated against a real file.
  - Filenames in `/recadv` and `/retann` are purely numeric, so retailer routing must be done on `BuyerParty` GLN/ILN with fail-closed behavior, mirroring the DocProcess routing model. The numeric FTP filename is a transport reference only; business dedupe keys are provider + doctype + `DocumentNumber`/`RetannNumber` + resolved retailer.
  - DESADV v4.1 (outbound) made `EAN` and `BuyerItemID` optional and added `BatchNumber` AN(200); the outbound generator must emit v4.1 and must not assume both identifiers are present.
- **DOCX re-verified in full 2026-07-27** (90 blocks incl. tables, via `python-executor` + lxml over `word/document.xml`). The first extraction was lossy; `Manual_integrare_facturare_edi_Auchan_Dedeman.md` now restores: the applicability section, the concept definitions, `CCCS1DXTRDRMTRL` column semantics (`MTRL` product, `TRDR` client, `CODE` client-specific code, one distinct `CODE` per `TRDR`), the fact that the PV de neconformitate is a **separate document** not all clients emit, the three concrete Auchan EAN-mismatch products (`MIAU MIAU ZGARDA PISICA FANCY`, `MIAU MIAU LOPATICA LITIERA`, `SP CUSCA TRANSPORT GULLIVER`), the 21% VAT rate, document dates, series 7121 for other clients, negative 9221 amounts, and the testable invariant that the PV quantity equals exactly shipped minus accepted. Lesson: always re-read the source DOCX rather than trusting a derived markdown.
- **`DOAR AVIZ` rule is explicitly provisional in the source**: the DOCX wording is "NU se integrează **(deocamdată)** în Soft1 ca și comenzi normale". This softens the contradiction with the observed production flow — the beneficiary never stated it as final, so the decision is genuinely open rather than a violation of a settled rule.
- **All four manual examples re-verified against production 2026-07-27** (MCP `soft1-petfactory`, read-only). Every figure held. New facts established:
  - **Document linking is `MTRLINES.FINDOCS` + `MTRLINES.MTRLINESS` only.** Header fields `CCCOrderId`/`CCCDispatcheId`/`CCCBillingReferenceId` are NULL throughout; `CCCORDERDOC` holds an internal `SOGRP .../SO ...` reference, not the EDI order number. `getTableFields` on `SALDOC` confirms `FINDOCS`/`MTRLINESS` are **readOnly** in both header and `ITELINES`, so links must be created via Soft1 document conversion, not `setData`.
  - **`FINDOC.NUM04` carries the EDI order number** (pointed out by the beneficiary, verified in production 2026-07-27) and propagates through conversion across the whole CKEY → advice → invoice → 9221 chain. Coverage over 2026-05-01 → 2026-07-27, series 7012/7111/7122/7123/9221: **100% of conversion-sourced documents have it** (Dedeman 604/604 advices, Auchan 156/156); the 101 Auchan advices without it have no `FINDOCS>0` line at all, i.e. they are non-EDI. Value is stored already normalized (`1436603`, not `01436603`). Caveats: it is a `float`, so it cannot hold alphanumeric or zero-significant order numbers; and on consolidated invoices the header keeps only **one** of the source orders (36 of 800 invoices consolidate 2–3 orders — e.g. `FAEXD-PF-39304` sources `4516694275` + `4516680594` but stores only the first). Use it as the fast order → document lookup key and set it on newly created documents, but keep `MTRLINES.FINDOCS` as the authoritative traceability path.
  - **`FINDOCL` is a different thing from `FINDOCS`, and it is writable.** `FINDOCS` = provenance (written by conversion, readOnly); `FINDOCL` = storno target ("which document does this line reverse", editable, editor `VSELASSLINES1`). `ON_POST` in `S1/JS/SALDOC_EF.js` makes `FINDOCL` **mandatory on series 9221 and 7531** and caps the quantity against `sum(qty1)` from `MTRTRN` for that target document + product. Measured on 2026 lines: 9221 has 940 lines, 0 without `FINDOCL`, 100% with `FINDOCS`, and `FINDOCL == FINDOCS` in 932 (99.1%) — so on 9221 it is a hand-written duplicate of the read-only link. 7531 is the opposite: 5304 lines, only 29% have `FINDOCS`, and when it targets a 7111 advice (2370 lines) `FINDOCS` is missing in 2369 — there `FINDOCL` is the only link. Consequence: for returns we do **not** need to solve the conversion problem, `FINDOCL`/`MTRLINESL` are scriptable. `MTRLINESL` is optional (764/940) and unused by the validation. **Known ERP gap:** the quantity cap is not cumulative — it never subtracts earlier returns, so the same advice line can be returned in full more than once; production holds **78 over-return cases**, 17 since 2025-01-01, latest 2026-06-30 (`AEX-AE-028769` / `MTRL 28072`: shipped 6, returned 18 across three documents). The automation must do its own cumulative check before issuing a 9221.
  - **The shortage mechanism is not "invoice in full, then credit".** The 7122/7123 invoice line already carries the accepted quantity; the 9221 carries only the difference; both point at the *same* advice line. Auchan `MTRL 40977`: advice 417 → invoice 416 + return 1. Dedeman `MTRL 44622`: advice 96 → invoice 48 + return 48.
  - **9221 amounts are stored positive** in `FINDOC.NETAMNT`/`SUMAMNT`; the minus sign is a presentation convention of the series.
  - **Advice numbers in the receipt note are our `FINCODE` stripped of series prefix and leading zeros**, slash-joined: `53184/53186` ⇔ `AEX-AE-053184`/`AEX-AE-053186`. Auchan `BuyerOrderNumber` arrives zero-padded to 8 (`01436603`). Both need explicit normalization.
  - The consolidated invoice `FAEXD-PF-39304` genuinely sources lines from two different advices, so per-line traceability survives consolidation.
- **EAN matching quantified and rejected as a fallback.** Over 119 distinct `BuyerItemID`/`GTIN` pairs from Auchan orders (2026-04 → 2026-07): `BuyerItemID` failure rate **0**; `GTIN` fails in 11 cases (9.2%). Two independent causes: Auchan sends a sibling variant's EAN (`424337`, `423804`, `423807`, `423802`, `341308`), and our own `MTRL.CODE1` is not unique (**167 duplicate EANs** in `COMPANY=50`). Implementation rule: `BuyerItemID → CCCS1DXTRDRMTRL.CODE` only; log `GTIN` divergence as a data-quality warning, never use it to resolve.
- **`CCCS1DXTRDRMTRL` real schema**: `MTRL`, `MSODTYPE`, `TRDR`, `TSODTYPE`, `LINENUM`, `CODE varchar(20)`, `NAME`, `COMMENTS`, `UnitPack`. **No `COMPANY` column.** Auchan mapping is strictly 1:1 (749/749, zero ambiguity); Dedeman has exactly one collision — `CODE=7050535` → `MTRL 34594` and `MTRL 34294`, both active — which must fail closed.
- **Client differences are business-level only (docx confirmed)**: base rules (shortage/surplus detection, consolidation) are common to Dedeman and Auchan; only invoice series (`7123` vs `7122`, plus `7121` for other EDI clients) and surplus handling (`Doar aviz` is Dedeman-only; Auchan physically refuses surplus) differ. Implementation consequence: one parser and one reconciliation engine plus a per-client config, not two pipelines.
- **Still unknown after the specs**: (a) where the PV de neconformitate / `Motiv de mutare` lives — RECADV only has optional `ReasonForReturnCode`/`ReasonForReturnDescription` and no PV number, and PV numbers in the examples have different prefixes than receipt numbers, so the working hypothesis is that PVs arrive as RETANN (5 files vs 91 fits); (b) shortage computation when optional `QuantityOrdered` is empty, which forces comparison against `7111` advice lines; (c) the Soft1 link/conversion operations for `7111 -> 9221 -> 7122/7123`; (d) whether Auchan actually sends the same v4.0 format, since both specs are Dedeman-scoped.

## Open Questions / Pending

- **MCP check 2026-06-05**: `SELECT DB_NAME()` through MCP returns `PetFactory`, but `sys.databases` includes `PetFactoryTEST`; `PetFactoryTEST.dbo.COMPANY` has `COMPANY=50`, `NAME='PET FACTORY SRL'`.
- **MCP check 2026-06-05**: `PetFactoryTEST.dbo.CCCRETAILERSCLIENTS` active company 50 row now has `WSURL='https://dev-petfactory.oncloud.gr/s1services'`.
- **SODATA compare 2026-06-05**: `CSTINFO.SODATA` is a Soft1/Delphi stream (`TPF0...TXCustomInfo...Data`). The JS starts at byte/char 263 and ends before two trailing `00` bytes. Final post-copy check: both `PetFactory` and `PetFactoryTEST` have extracted code length 35,722 bytes and SHA-256 `DBF62F4F23050DF3CE25047C38B6FDDB23599D25474E959A9965839202959262`, matching local `S1/JS/AJS/JSRetailers.js` encoded as Windows-1252. `SOUPDDATE`: prod `2026-06-05 16:31:34`, test `2026-06-05 16:47:16`.
- **Final AJS sync 2026-06-05**: after the `CAST(:1 AS VARCHAR(MAX))` filename fix, both `PetFactory` and `PetFactoryTEST` have extracted AJS length 35,812 bytes and SHA-256 `F11913C90D9BEE83AF909F1C5D12755627D13FC11E3C85FCC7707DF23BD9973B`, matching local `S1/JS/AJS/JSRetailers.js`. `SOUPDDATE`: prod `2026-06-05 16:55:59`, test `2026-06-05 16:55:30`.
- **Live test 2026-06-05**: real DocProcess XML from `documentatie/ORDERS_DX01_144_20260403_01004728_VAT_RO17275880_DX.xml`; real lookup values resolved in `PetFactoryTEST` (`TRDBRANCH=8500` for GLN `5949065001155`, `MTRL=44857` for buyer item `098625-0`). Flow result: `CCCSFTPXML=7148` `NEW->PROCESSING->SENT`, `FINDOC=2135459`, `FINCODE=CKEY-00059773`.
- **Live test 2026-06-05**: real Infinite/Dedeman XML from `documentatie/infinite_samples/orders/DEDEMAN_181059380.xml`; real lookup values resolved in `PetFactoryTEST` (`TRDBRANCH=7875` for GLN `5949111999818`; all 17 `BuyerItemID` values resolve to MTRL). Flow result: `CCCSFTPXML=7149` `NEW->PROCESSING->SENT`, `FINDOC=2135460`, `FINCODE=CKEY-00059774`, 17 lines.
- Add CCCDOCUMENTES1MAPPINGS row for SERIES=7531 RETANN and complete non-Dedeman Infinite mappings as needed.
- **Resolved 2026-06-05**: Auchan active TRDR is `13248`. Auchan and Dedeman now have individual Infinite `CCCSFTP` rows in both DBs. ORDER mappings are complete in both DBs (`CCCDOCUMENTES1MAPPINGS=43` Dedeman, `49` Auchan, 7 XML rows each). Dashboard cards for both are present.
- **Resolved 2026-06-05**: synthetic numeric Auchan row `PetFactoryTEST.dbo.CCCSFTPXML=7153` was processed successfully after looking up the real PK by filename; it is now `XMLSTATUS=SENT`, `FINDOC=2135462`.
- Heroku config vars: `EDINET_P12_BASE64`, `EDINET_P12_PASSWORD`, optional `EDI_SCAN_INTERVAL_MS`, `EDI_DOWNLOAD_AGE_DAYS`, `EDI_PROCESS_AGE_DAYS`, `EDI_PROCESS_BATCH`, `EDI_SCANNER`.
- Infinite RETANN/RECADV ingestion — live files are now available but intentionally untouched. The XML schemas are documented as of 2026-07-27 (`documentatie/dedeman/Infinite_EDInet_DESADV_RECADV.md`), so parsers can be written locally. Still required before enabling downloads: numeric-filename acceptance, `/retann` (singular) routing, retailer resolution by GLN/ILN, dedupe, DO-first backup, and the correct Soft1 business-document creation flow. Do not treat RECADV as DocProcess APERAK or insert it into `CCCAPERAK`; `parseAperak()` must stop being the RECADV entry point.
- RECADV billing semantics are now documented, but implementation still needs one representative Infinite XML plus confirmation of the Soft1 links/conversion operations for `7111 -> 9221 -> 7122/7123`. ~~Also decide the authoritative grouping key when receipt advice references and line order references conflict.~~ **Resolved 2026-07-27**: lines are authoritative because the header `DeliveryDocumentNumber` is truncated by spec.
- Resolve the `DOAR AVIZ` contradiction: manual behavior is "not integrated as CKEY **for now**" (explicitly provisional in the DOCX); observed production behavior created CKEY `CKEY-00061000`, then a second advice/invoice and a 7531 return of the old invoice. ~~Also correct the Dedeman matching specification: whether RECADV carries EAN or customer code determines `MTRL.CODE1` lookup versus direct `CCCS1DXTRDRMTRL.CODE` lookup.~~ **Resolved 2026-07-27**: RECADV v4.0 makes `BuyerItemID` mandatory and `GTIN` optional, so both retailers use `BuyerItemID -> CCCS1DXTRDRMTRL.CODE`.
- Invoice outbound flow (Infinite) — `sign-smime.js` ready, upload step not yet integrated.
- Before retiring `retailers1`, remove the remaining backend legacy fallback: delete/disable the `EDI_SCANNER=legacy|both` branch, then delete `src/services/sftp/*` and `storeXml` once `edi-aperaks` no longer delegates to `sftp.downloadXml/storeAperakInErpMessages` or those methods are moved into the dedicated service.
  - **Code intersections found 2026-07-27 (no code changed).** Product matching is *already* `BuyerItemID → CCCS1DXTRDRMTRL.CODE` for both retailers — it lives in the DB table `CCCXMLS1MAPPINGS`, read by `src/edi/order-builder.js`, not in code; so "never fall back to EAN" is a preservation constraint, not a new feature. `SALDOC.NUM04 ← Order/OrderHeader/BuyerOrderNumber` has **no transform SQL**, so the zero-padding normalization is a side effect of the `float` column, not deliberate. `runMappingSql` in `S1/JS/AJS/JSRetailers.js` uses `X.SQL` (first row, no `ORDER BY`), so the three `CCCS1DXTRDRMTRL` code collisions — Carrefour `112`, Dedeman `7050535`, Cora `334` — resolve silently; 24 Dedeman orders already carry `7050535` and it currently resolves to the live article `34294` by luck. No `MTRL` has two codes for one `TRDR`, so the outbound joins cannot duplicate lines. The new `exportXMLDedemanReturn()` in `S1/JS/SALDOC_EF_27072026.js` (cmd `20260511`, Dedeman + 7531) derives `RetAnnNumber`/`RetAnnDate`/`DeliveryDate`/`DeliveryDocumentNumber` **all from `A.findocl`** and negates every value — independent confirmation of both the `FINDOCL` role and the stored-positive finding; its `RefInvoiceNumber` comes from `FINDOCS` but is unvalidated and will be silently empty for the 71% of 7531 lines that have none. Also noted: cmd `20260121` sets `SERIES.SOISCONV=false` on 9221 with no restore code anywhere; the `CCCUNITPACK` update in `ON_AFTERPOST` builds malformed SQL (missing spaces around `WHERE`/`AND`); `S1/JS/EFIntegrareRetailers.js` is an older duplicate of the same handlers.

## Next Step

1. Implement `parseRecadv()` and `parseRetann()` in `src/edi/providers/infinite.provider.js` against the now-documented v4.0 schemas, plus synthetic fixtures derived from the specs and unit tests. This is fully local: do NOT enable scanner prefixes for `/recadv` or `/retann` and do NOT `RETR` any live file. Note the required code changes: `remoteSubdir('retann')` must become `/retann/` (live path is singular), the numeric-filename filter must replace the `AUCHAN_`/`DEDEMAN_` prefix check for these two doc types, retailer routing must use `BuyerParty` GLN/ILN with fail-closed behavior, and `parseAperak()` must stop being used for RECADV.
2. Validate the parsers against one representative real RECADV and one RETANN obtained via a non-consuming EDINet/portal export. `documentatie/dedeman/RetAnn.xml` is Infinite's own spec example and can seed the RETANN fixture, but it does not prove what the account actually delivers. A controlled one-file `RETR` with immediate DO backup is the fallback only if no export is available. Confirm Auchan uses the same v4.0 format.
3. Confirm with the beneficiary the two remaining business decisions before any document creation: the `DOAR AVIZ` rule (block before CKEY vs reproduce the observed historical flow) and who creates the 7531 return for surplus. **Partially resolved 2026-07-27:** the return/storno link (`9221`/`7531` → source doc) does not need a conversion mechanism — `MTRLINES.FINDOCL`/`MTRLINESL` are writable and already used for exactly this by `S1/JS/SALDOC_EF_27072026.js`. What remains open is only the **invoice** side (`7111 → 7122/7123`), where `FINDOCS`/`MTRLINESS` are read-only and the actual Soft1 conversion mechanism used today has not yet been identified.
4. APERAK DO retry is fully resolved and verified 2026-06-11: backend v116 (`6562a244`) processes APERAK in the DO retry loop, ERP AJS `createAperak` uses `OUTPUT INSERTED.CCCAPERAK` (live probe returned real PK `7232`, not 0), and DO `retry/` is confirmed empty (`RETRY_TOTAL=0`). No further action needed for this issue.
5. For Kaufland order `3200164514` / filename sequence `01003727`, query S1 `orders-log` (`TRDR_RETAILER=12349`, 2026-06-18..19, `OPERATION='mappingError'`) and correlate the `CCCSFTPXML` row. Validate `XMLDATA` (or DO backup) and only then deploy this backend change and use individual `Retrimite`; verify `SENT` plus `FINDOC` or the new actionable error.
6. Monitor `retailers4` logs after cutover for first real inbound XML: expected startup lines are `[scanner] new EDI scanner ENABLED (multi-provider)` and `[do-storage] retry loop ENABLED (300000ms)`.
7. If rollback is needed, set `retailers4 ENABLE_SFTP_SCANNER=false DO_RETRY_INTERVAL_MS=0`, then set `retailers1 ENABLE_SFTP_SCANNER=true`.
