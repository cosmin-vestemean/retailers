node_modules/
todos/
.vscode/
.main-worktree

# public/dist/ ramane versionat intentionat ca fallback, desi de la introducerea
# `heroku-postbuild` (2026-08-27) Heroku ruleaza `vite build` proaspat la fiecare deploy.

# Generat pe Windows: contine binary-uri opționale (native rolldown) specifice platformei,
# ceea ce sparge instalarea pe dyno-ul Linux al Heroku (npm/cli#4828). npm install (nu ci)
# rezolva dependentele corect per-platforma daca lockfile-ul lipseste.
frontend/package-lock.json

# Secrete - nu commita niciodata
.env
.env.*
!.env.example
privateKey.txt
*.pem
*.key
*.p12
documentatie/PET_FACTORY_EC_KIT/pass.txt
documentatie/dedeman/PET_FACTORY_EC_KIT/pass.txt

# Dump-uri de log Heroku: contin credentiale de productie si date de client
heroku-logs/

# Payload-uri reale capturate de pe FTP-ul Infinite (date de productie)
documentatie/infinite_samples/recadv/
documentatie/infinite_samples/retann/

# Fisierele descarcate live de scanner odata cu cuplarea RECADV (date reale de retailer)
data/infinite/*/recadv/
data/infinite/*/retann/
